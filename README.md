# 🚲 Bike Rental Service

Сервис для автоматизации пункта проката велосипедов

## 🛠 Стек технологий

* **Platform**: .NET 8 (C# 12)
* **Database**: PostgreSQL
* **ORM**: Entity Framework Core 8
* **Messaging**: NATS JetStream (NATS.Net)
* **Mapping**: AutoMapper
* **Testing**: xUnit, Bogus (Fake Data)

## 📂 Структура проекта

### 1. BikeRental.Domain
Доменная модель. Содержит доменные сущности и перечисления
* **Entities**: `Bike`, `BikeModel`, `Renter`, `Rental`
* **Enum**: `BikeType`

### 2. BikeRental.Application
Бизнес-логика приложения.
* **Contracts**: DTO (records) для CRUD и аналитики
* **Services**: Реализация бизнес-правил (`BikeService`, `AnalyticsService` и др.)
* **Mapping**: Профили AutoMapper

### 3. BikeRental.Infrastructure
Реализация работы с внешними системами
* **EfCore**: Конфигурация `DbContext`, миграции, реализация репозиториев
* **Nats**: Реализация `Producer` (отправка событий) и `Consumer` (обработка батчей аренды)

### 4. BikeRental.Api.Host
Точка входа (Web API)
* **Controllers**: REST API контроллеры (наследуются от `CrudControllerBase`)

### 5. BikeRental.Generator
Сервис генерации нагрузки
* **Worker**: `BackgroundService`, генерирующий фейковые данные через **Bogus**
* **Flow**: Генерирует пачки аренд -> Публикует в NATS -> Api.Host считывает и сохраняет в БД